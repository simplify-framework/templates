const path = require('path')
module.exports = {
    entry: {
        index: './src/Functions/functions.js',
    },
    output: {
        path: path.join(__dirname, 'layer/nodejs/node_modules/{{NameSnake}}'),
        publicPath: '/',
        filename: '[name].js',
        libraryTarget: 'umd'
    },
    target: 'node',
    node: {
        // Need this when working with express, otherwise the build fails
        __dirname: false,     // if you don't put this is, __dirname
        __filename: false,    // and __filename return blank or /
    },
    externals: [], // Dont need this to avoid error with all internal modules
    module: {
        rules: [
            {
                // Transpiles ES6-8 into ES5
                test: /\.js$/,
                exclude: [
                    /node_modules/
                ],
                use: {
                    loader: "babel-loader"
                }
            },
            {
                // Transpiles files into memory
                test: /\.yaml$/,
                exclude: [
                    /node_modules/
                ],
                use: {
                    loader: "file-loader"
                }
            }
        ]
    },
    plugins: []
}