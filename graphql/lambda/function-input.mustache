{
    "Profile": "{{DeploymentProfile}}",
    "Region": "{{DeploymentRegion}}",
    "AccountId": "{{DeploymentAccount}}",
    "Environment": "{{DeploymentEnvSnake}}",
    "Bucket": {
        "Name": "{{DeploymentBucket}}-{{ProjectId}}-{{DeploymentEnvSnake}}",
        "Key": "functions/${DATE_TODAY}"
    },
    "OutputFile": "{{DeploymentEnvSnake}}/{{FunctionNamePascal}}-meta.json",
    "LayerConfig": {
        "CompatibleRuntimes": [
            "{{Runtime}}"
        ],
        "LayerName": "{{ProjectName}}{{DeploymentEnvPascal}}-{{FunctionName}}"
    },
    "Function": {
        "FunctionName": "{{FunctionName}}",
        "Handler": "index.handler",
        "MemorySize": 128,
        "Publish": true,
        "Role": "${Function{{FunctionNamePascal}}RoleArn}",
        "Runtime": "{{Runtime}}",
        "Tags": {
            "GroupName": "{{ProjectNamePascal}}{{DeploymentEnvPascal}}"
        },
        "Timeout": 15,
        "TracingConfig": {
            "Mode": "PassThrough"
        },
        "Environment": {
            "Variables": {
                "DEPLOYMENT_STAGE": "${DEPLOYMENT_STAGE}",
                "ENFORCEMENT_PACKAGE": "${ENFORCEMENT_PACKAGE}",
                "DEPLOYMENT_ENV": "${DEPLOYMENT_ENV}"
            }
        }
    }
}